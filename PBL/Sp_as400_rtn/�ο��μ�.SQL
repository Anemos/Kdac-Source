  SELECT AA."XCMCD" AS XCMCD,   
         AA."XINEMP" AS XINEMP, 
			( SELECT BB.PENAMEK FROM "PBCOMMON"."DAC003" BB WHERE BB.PEEMPNO = AA.XINEMP ) AS XINEMPNAME,
         AA."XPLEMP",   
         AA."XMACADDR",   
         AA."XIPADDR",   
         AA."XINDT"  
    FROM "PBRTN"."RTN019" AA  

  SELECT "PBCOMMON"."DAC003"."PEEMPNO",   
         "PBCOMMON"."DAC003"."PENAMEK",   
         "PBCOMMON"."DAC003"."PEJIKCHEK",   
         "PBCOMMON"."DAC003"."PEDEPT",   
         "PBCOMMON"."DAC003"."PECLASS",   
         "PBCOMMON"."DAC002"."COITNAME"  
    FROM "PBCOMMON"."DAC003",   
         "PBCOMMON"."DAC002"  
   WHERE ( "PBCOMMON"."DAC003"."PECLASS" = "PBCOMMON"."DAC002"."COCODE" ) and  
         ( ( "PBCOMMON"."DAC003"."PEDEPT" = :ag_dept ) AND  
         ( "PBCOMMON"."DAC002"."COGUBUN" = 'PER306' ) AND  
         ( "PBCOMMON"."DAC002"."COMLTD" = '01' ) )    AND  "PBCOMMON"."DAC003"."PEOUT"  <> '*' 

SELECT "PBCOMMON"."DAC001"."DCODE" ,"PBCOMMON"."DAC001"."DNAME"
          
    FROM "PBCOMMON"."DAC001"  
   WHERE ( "PBCOMMON"."DAC001"."DUSE" = ' ' ) and
	      ( "PBCOMMON"."DAC001"."DTODT" = 0  ) and
	      ( "PBCOMMON"."DAC001"."DACTTODT" = 0 )  and
		   ( "PBCOMMON"."DAC001"."DDIV2" <> '' ) and
		   ( "PBCOMMON"."DAC001"."DFNAME1" <> '외주업체' )
		   
		   
SELECT AA."XCMCD" AS XCMCD,   
         AA."XINEMP" AS XINEMP, 
			( SELECT BB."PENAMEK" FROM "PBCOMMON"."DAC003" BB WHERE BB."PEEMPNO" = AA."XINEMP" ) AS XINEMPNAME,
			( SELECT BB."PEDEPT" FROM "PBCOMMON"."DAC003" BB WHERE BB."PEEMPNO" = AA."XINEMP" ) AS XINEMPDEPT,
			( SELECT CC."DNAME" FROM "PBCOMMON"."DAC003" BB, "PBCOMMON"."DAC001" CC 
				WHERE ( CC."DCODE" = BB."PEDEPT" ) and ( CC."DUSE" = ' ' ) and ( CC."DTODT" = 0  ) and
	      		( CC."DACTTODT" = 0 )  and ( CC."DDIV2" <> '' ) and
		   		( CC."DFNAME1" <> '외주업체' ) AND ( BB."PEOUT" <> '*' ) ) AS XINEMPDEPTNAME,
			( SELECT DD."COITNAME" FROM "PBCOMMON"."DAC002" DD, "PBCOMMON"."DAC003"BB
				WHERE ( BB."PECLASS" = DD."COCODE" ) and  
         			( ( BB."PEEMPNO" = AA."XINEMP" ) AND  
         			( DD."COGUBUN" = 'PER306' ) AND  
         			( DD."COMLTD" = '01' ) )    AND  BB."PEOUT"  <> '*'  ) AS XINEMPCLASSNAME,		
         AA."XPLEMP" AS XPLEMP, 
			( SELECT BB."PENAMEK" FROM "PBCOMMON"."DAC003" BB WHERE BB."PEEMPNO" = AA."XPLEMP" ) AS XPLEMPNAME,
			( SELECT DD."COITNAME" FROM "PBCOMMON"."DAC002" DD, "PBCOMMON"."DAC003"BB
				WHERE ( BB."PECLASS" = DD."COCODE" ) and  
         			( ( BB."PEEMPNO" = AA."XPLEMP" ) AND  
         			( DD."COGUBUN" = 'PER306' ) AND  
         			( DD."COMLTD" = '01' ) )    AND  BB."PEOUT"  <> '*' ) AS XPLEMPCLASSNAME,
         AA."XMACADDR",   
         AA."XIPADDR",   
         AA."XINDT"  
  FROM "PBRTN"."RTN019" AA 
  
  
SELECT RCPLANT,RCDVSN,RCITNO,RCLINE1,RCLINE2,RCOPSQ,RCOPNO,RCOPNM,RCLINE3,RCBMTM,RCBLTM,RCBSTM
FROM PBRTN.RTN013;